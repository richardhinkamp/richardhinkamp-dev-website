{% extends "layout/layout.twig" %}

{% block page_content %}
    <article>
        <div class="meta">
            <h1>{{ page.title }}</h1>
        </div>
        {{ page.body }}
    </article>
{% endblock %}

