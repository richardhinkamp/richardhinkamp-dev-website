<article class="news-intro">
    {% if record.image!="" %}
        <div class='imageholder'><img src="{{ record.image|thumbnail(120, 120) }}">
        </div>
    {% endif %}

    <h2><a href="{{ record.link }}">{{ record.title }}</a></h2>
    <p class="meta">Geplaatst op {{ record.datepublish|date("d-m-Y")}}</p>

    {{ record.excerpt(300) }}

    <p class="read-more"><a href="{{ record.link }}">Lees verder &raquo;</a></p>
</article>